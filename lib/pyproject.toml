[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "exo"
version = "0.1.0"
description = "Shared Python library for exo developer tooling"
requires-python = ">=3.12"

[tool.hatch.build.targets.wheel]
packages = ["src/exo"]
